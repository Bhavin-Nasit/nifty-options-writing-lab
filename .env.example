# Optional dashboard tuning. Defaults are shown here.
NSE_CACHE_SECONDS=900
NIFTY_LOT_SIZE=65

# Optional only for local historical Kite data capture scripts, not required by the hosted dashboard.
KITE_API_KEY=
KITE_ACCESS_TOKEN=
